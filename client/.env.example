# Copy to .env next to compose.yml and fill in the join code shown by the
# Pelican Auto Proxy plugin's Setup page (or by install-vps.sh's peer
# command). Placeholder only - never commit a real join code.
AUTOPROXY_JOIN_CODE=
